pipeline {
    agent any
    tools {
        maven 'Maven 3.6.3'
    }
    stages{
        stage('Build') {
            steps {
                sh './mvnw clean install -DskipTest'
            }
        }
        stage('Test'){
            steps {
                sh './mvnw test -Punit'
            }
        }
        stage('Deliver') {
            steps {
                echo 'Delivering...'
            }
        }
        stage('Deploy') {
            steps {
                sh 'mvn spring-boot:run'
            }
        }
    }
}